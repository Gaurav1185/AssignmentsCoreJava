package com.ClassNotes;

public class Enum {

    //enum set of constants (specify own constants)using enum we are going to define enum
    // also store like array oth position 1st position (in java all constants should be UPPERCASE)
    //we can create enum object
    //values,name string return type,ordinary  three methods are there

    //

}
